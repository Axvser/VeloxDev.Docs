# TypeScript / JavaScript Documentation Conventions

## Applicable Scenario

Load this constraint when `package.json` + `tsconfig.json` (TypeScript) or `package.json` only (JavaScript) is detected.

## Constraint Rules

(Content to be defined.)
