# AOP — API Reference

> **Evidence:** Demo (`Examples/AOP/WPF/Demo`, `Examples/AOP/Avalonia/Demo`) + runtime source (`Src/Core/VeloxDev.Core/AspectOriented/*.cs`, `Src/Core/VeloxDev.Core/Interfaces/AspectOriented/IAspectOriented.cs`) + generator source (`Src/Generators/VeloxDev.Core.Generator/AopInterface.cs`, `AopProxy.cs`, `Writers/AopWriter.cs`).
> **Note on tests:** no AOP-specific test suite currently exists under `VeloxDev.Core.Test` (the Feature Inventory's `Demo + Test` entry for AOP is stale). All signatures below come from Demo usage and the runtime/generator source.

## API — Sections

This feature's API reference is split into:

- `00_aspect-oriented/`
- `01_generated-api/`
