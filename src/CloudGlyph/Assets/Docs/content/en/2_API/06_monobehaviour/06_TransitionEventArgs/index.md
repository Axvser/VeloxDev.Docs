# MonoBehaviour — `TransitionEventArgs`

```csharp
public sealed class TransitionEventArgs : TimeLineEventArgs { }
```

**Notes:**
- A marker event-args type used by the Transition system; inherits `Handled` (verified by `TimeLineEventArgsTests.TransitionEventArgs_*`).

## `VeloxDev.MonoBehaviour`
