# MonoBehaviour — `TransitionEventArgs`

```csharp
public sealed class TransitionEventArgs : TimeLineEventArgs { }
```

**说明：**
- 过渡系统使用的标记事件参数类型；继承 `Handled`（由 `TimeLineEventArgsTests.TransitionEventArgs_*` 验证）。

## `VeloxDev.MonoBehaviour`
